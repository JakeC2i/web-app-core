# Basic TypeScript project template

This is a template.

1. Clone it
2. Delete .git folder
3. Change `.gitignore` according to comments
4. Change project info in `package.json`
5. Initialize new Git repo with `git init`
6. Change this readme
7. Done
